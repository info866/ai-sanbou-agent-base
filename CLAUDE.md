# CLAUDE.md

This repo's agent-facing rules live in [AGENTS.md](AGENTS.md). Read once
before non-trivial work; call `issue_rag_briefing(owner="info866",
repo="ai-sanbou-agent-base")` to fetch project context + past lessons + open
failures + acceptance criteria in a single round trip.
